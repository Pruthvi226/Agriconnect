<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd
           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Enable AspectJ AOP -->
    <aop:aspectj-autoproxy />

    <!-- Component Scan for Services, DAOs, Components -->
    <context:component-scan base-package="com.agriconnect.service, com.agriconnect.dao, com.agriconnect.security, com.agriconnect.util, com.agriconnect.config" />

    <!-- Shared executor/scheduler beans used by @EnableAsync and @EnableScheduling -->
    <task:executor id="taskExecutor" pool-size="5" />
    <task:scheduler id="taskScheduler" pool-size="5" />

    <!-- Load base properties; production overrides are supplied via ENV vars which
         map to the ${..} placeholders already defined in application.properties. -->
    <context:property-placeholder
        location="classpath:application.properties"
        ignore-unresolvable="true"
        ignore-resource-not-found="true"
        order="1" />

    <!-- Production-grade pooled DataSource. -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}" />
        <property name="jdbcUrl" value="${jdbc.url}" />
        <property name="username" value="${jdbc.username}" />
        <property name="password" value="${jdbc.password}" />
        <property name="maximumPoolSize" value="${jdbc.pool.maximumPoolSize}" />
        <property name="minimumIdle" value="${jdbc.pool.minimumIdle}" />
        <property name="connectionTimeout" value="${jdbc.pool.connectionTimeoutMs}" />
        <!-- -1 means: don't fail startup if DB is unreachable. App starts fast
             and serves /health immediately; DB-dependent endpoints fail gracefully. -->
        <property name="initializationFailTimeout" value="${jdbc.pool.initializationFailTimeout:-1}" />
        <property name="poolName" value="AgriConnectPool" />
    </bean>

    <!-- Hibernate SessionFactory -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <!-- Map config file instead of packages to explicitly use hibernate.cfg.xml as requested -->
        <property name="configLocation" value="classpath:hibernate.cfg.xml" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${hibernate.dialect}</prop>
                <prop key="hibernate.show_sql">${hibernate.show_sql}</prop>
                <prop key="hibernate.format_sql">${hibernate.format_sql}</prop>
                <prop key="hibernate.hbm2ddl.auto">${hibernate.hbm2ddl.auto}</prop>
                <prop key="hibernate.default_schema">${hibernate.default_schema}</prop>
            </props>
        </property>
    </bean>

    <!-- Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory" />
    </bean>

    <!-- Enable annotation-based transaction management with CGLIB proxying -->
    <tx:annotation-driven transaction-manager="transactionManager" proxy-target-class="true" />

</beans>
